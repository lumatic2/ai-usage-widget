node_modules/
npm-debug.log*
package-lock.tmp
dist/
out/
release/

# Tauri / Rust build artifacts
target/
**/target/
*.pdb

# Local archive of legacy Electron sources (kept locally, not tracked)
archive/*

# Project-local notes (kept on disk, not tracked in public repo)
CLAUDE.local.md
CLAUDE.md
ROADMAP.md
docs/BUGLOG.md

# OS / editor cruft
.DS_Store
Thumbs.db

# Local screenshots or experiments
*.log

# Personal shortcuts (contain hardcoded local paths)
create_shortcut.ps1

# Claude Code local state
.claude/

# 완료 노트는 폐기물이 아니라 milestone 완료의 정본 증거다 (하네스 배치 규약).
# archive/ 를 폐기물 창고로 쓰더라도 이 경로는 추적한다.
!archive/reports/
